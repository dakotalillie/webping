name: cd
on:
  push:
    branches: [ci]
jobs:
  validate:
    uses: ./.github/workflows/validate.yml
  deploy-to-prod:
    needs: validate
    uses: ./.github/workflows/deploy.yml
    with:
      stack: prod
